import {Route} from '@angular/router';
import {TagFeedComponent} from './components/tag-feed.component';

export const tagFeedRoute: Route[] = [
    {
        path: '',
        component: TagFeedComponent,
    },
];
